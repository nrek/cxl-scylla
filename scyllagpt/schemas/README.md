# Protocol schemas

Generated from the Codex app-server CLI:

```text
codex app-server generate-json-schema --out ./schemas
```

These files are a protocol snapshot, not a bundled runtime. Regenerate and review them when updating the recommended Codex version.
